# HansRobot 离线测试夹具

`datasheet-v6.json` 按厂家《HuayanRobotV6控制通信协议接口 V1.0.19.1》正文第 257～260 页的字段和数值整理；状态改为就绪，设备号是 **FAKE-E05-001**，仅用于测试，不是真机抓包。

协议没有注明 DataSheet 长度字段字节序。测试分别覆盖大端和小端；真实字节序须在后续只读联调确认，不能凭这些夹具推断。
