## mac
```bash
# 将mac端口18001映射到服务器（5090）的端口18011上
ssh -N -o ExitOnForwardFailure=yes \
  -L 18001:127.0.0.1:18011 5090
```

```bash
# 生成probe（探针）
PYTHONPATH="$PWD/packages/surgical_contracts:$PWD" \
  /Users/bigcat/Documents/internS/venv/course/bin/python \
  scripts/edge/probe_huayan_read_only.py \
  --real-config configs/robot-real.local.yaml \
  --connect-real-read-only \
  --vendor-compatibility-confirmed \
  --operator-ready \
  --byte-order little

# 启动网关
cd /Users/bigcat/Documents/internS/interns2-finetune
EDGE_PYTHON=/Users/bigcat/Documents/internS/venv/course/bin/python \
  ./scripts/edge/start_gateway.sh \
  --real-config configs/robot-real.local.yaml \
  --probe-summary "把刚输出的raw local record路径填这里/read-only-summary.json" \
  --connect-real-read-only --vendor-compatibility-confirmed \
  --operator-ready --datasheet-byte-order little \
  --server-url ws://127.0.0.1:18001/v1/gateway/connect \
  --secret-file configs/gateway-auth.local \
  --gateway-id mac-huayan-ph590320009
```