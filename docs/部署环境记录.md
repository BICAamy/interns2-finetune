# 手术导航项目部署环境记录

> 用途：记录 `docs/手术导航技术实现流程文档.md` Step 0 的服务器基线与验收状态  
> 数据来源：用户于 2026-08-13 在 `xl@ubuntu-Rack-Server` 执行的只读检查命令  
> 记录状态：已建立初始基线，Step 0 尚未通过验收  
> 安全说明：本文不记录 API Key、密码或其他认证凭据

## 1. Step 0 当前结论

当前服务器具备继续开展项目的主要硬件和 Docker 基础：

- Ubuntu x86_64；
- 4 张大显存 NVIDIA GPU；
- NVIDIA 驱动和 CUDA 12.8 环境；
- Docker Engine 正常；
- 已存在 InternS2 LMDeploy 容器和模型只读挂载；
- 项目仓库及模型目录已挂载进该容器。

但 Step 0 暂时不能标记完成，原因如下：

1. `docker compose version` 返回 `docker: unknown command: docker compose`；
2. `xl_interns2_lmdeploy` 当前主进程是 `sleep infinity`，不是 LMDeploy API Server；
3. 容器内执行 `lmdeploy version` 返回“未找到命令”；
4. 容器内访问 `http://127.0.0.1:23333/v1/models` 失败；
5. 因模型服务未启动，当前无法记录 `/v1/models` 返回的模型 ID；
6. 服务器 Git 工作区包含尚未确认归属和用途的改动；
7. 8000、8001、8002、23333 端口占用情况尚未完整检查；
8. EGL、OpenGL 和 Xvfb 可用情况尚未检查。

在以上问题被确认前，不进入会修改 `agent/README.md` 的 Step 1。

## 2. Git 基线

### 2.1 仓库位置

```text
/home/xl/interns2-finetune
```

### 2.2 当前提交

```text
14c1256d7745d0ae99804a497ce69259dca16541
```

简短提交记录：

```text
14c1256 NavGPT的DS测试
03fb899 拉取NavGPT
4104c55 xtuner工具来了
7dc2e63 标准与FP8模型正常推理成功
e595b93 init: 项目初始化，包含 Intern-S2-Preview-FP8 模型配置和测试脚本
```

分支状态：

```text
main，与 origin/main 指向相同提交
```

### 2.3 未提交改动

```text
 M agent/README.md
?? images/
```

这两项属于服务器工作区中的既有改动，当前不能删除、覆盖或自动暂存。尤其 Step 1 本来就要重写 `agent/README.md`，因此必须先确认现有修改是否需要保留。

需要补充以下只读输出：

```bash
cd ~/interns2-finetune
git diff -- agent/README.md
find images -maxdepth 2 -type f -print
```

确认内容后应选择以下一种方式：

- 需要保留：先单独提交；
- 只是临时文件：由用户明确决定如何处理；
- 与新架构有关：在 Step 1 中人工合并，不能直接覆盖。

## 3. 操作系统与硬件

### 3.1 操作系统

| 项目 | 已确认值 |
|---|---|
| 主机名 | `ubuntu-Rack-Server` |
| 内核 | `6.14.0-37-generic` |
| Ubuntu 系列 | 命令输出显示 `24.04.1` 内核构建标识 |
| CPU 架构 | `x86_64` |

`x86_64` 满足当前固定版 `sofa_env` 预构建 SOFA 路线的架构前提。

### 3.2 GPU

| 项目 | 已确认值 |
|---|---|
| GPU 数量 | 4 |
| 型号 | `NVIDIA RTX PRO 6000 Blac...`，完整名称待补充 |
| 单卡显存 | 97887 MiB，约 96 GiB |
| NVIDIA Driver | `570.153.02` |
| `nvidia-smi` 报告 CUDA | `12.8` |
| MIG | Disabled |

补充完整 GPU 名称：

```bash
nvidia-smi --query-gpu=index,name,memory.total,driver_version --format=csv
```

### 3.3 采样时的 GPU 使用情况

采样时间：2026-08-13 14:11:01（服务器终端时间）。

| GPU | 已用显存 | GPU 利用率 | 主要计算进程 |
|---:|---:|---:|---|
| 0 | 53380 MiB | 93% | `.../venvs/navgpt-rl/bin/python`，PID 1266050 |
| 1 | 52772 MiB | 93% | `.../venvs/navgpt-rl/bin/python`，PID 1266051 |
| 2 | 52926 MiB | 85% | `python`，PID 1264287 |
| 3 | 52926 MiB | 84% | `python`，PID 1264288 |

每张卡还存在一个约 4 MiB 的 Xorg 图形进程。

注意事项：

- 四张 GPU 在采样时均处于高负载；
- 这些进程可能属于其他实验，不得停止或修改；
- 后续启动 InternS2 或 GPU 渲染前必须重新执行 `nvidia-smi`；
- 仿真第一版优先使用 CPU 物理与离屏/软件渲染，避免与现有训练任务争抢显存；
- InternS2 使用哪些 GPU 必须在实际启动命令中显式确认。

## 4. Docker 基线

### 4.1 Docker Engine

| 项目 | 已确认值 |
|---|---|
| Docker Client | `28.2.2` |
| Docker Server | `28.2.2` |
| API | `1.50` |
| containerd | `1.7.28` |
| runc | `1.3.3-0ubuntu1~24.04.3` |
| 存储驱动 | `overlay2` |
| OS/Arch | `linux/amd64` |

Docker Engine 工作正常。

### 4.2 Docker Compose

执行结果：

```text
docker: unknown command: docker compose
```

当前不能使用文档中的 `docker compose ...` 命令。先执行以下只读检查，确认服务器是否安装了旧版独立命令或哪个 Compose 包可用：

```bash
docker-compose version
apt-cache policy docker-compose-v2
apt-cache policy docker-compose-plugin
```

根据检查结果再选择与当前 Ubuntu/Docker 安装来源匹配的 Compose v2 安装方式。安装完成标准：

```bash
docker compose version
```

能够返回 Compose v2 版本。

### 4.3 采样时正在运行的容器

| 容器名 | 镜像 | 状态 | 已发布端口 |
|---|---|---|---|
| `full-distill-gpu23-cdi` | `full-distill:gpu23-snapshot-20260810` | Up 2 days | 无宿主机发布 |
| `full-distill-gpu01-cdi` | `dd94fce2f83a` | Up 2 days | 无宿主机发布 |
| `full-distill-gpu01` | `dd94fce2f83a` | Up 2 days | 无宿主机发布 |
| `rl-navgpt` | `rl-navgpt:pre-egl-20260807` | Up 21 hours | 无宿主机发布 |
| `xl_interns2_lmdeploy` | `openmmlab/lmdeploy:v0.14.0-cu12.8` | Up 2 days | 无宿主机发布 |
| `lx` | `20c5b3b08c58` | Up 2 days | 9520～9530 |
| `wcz_torch_260108` | `nvcr.io/nvidia/pytorch:25.12-py3` | Up 4 hours | 18888～18898 |

现有容器均不能被本项目擅自停止、重建或删除。

## 5. 现有 InternS2 容器

### 5.1 容器身份

| 项目 | 已确认值 |
|---|---|
| 容器名 | `xl_interns2_lmdeploy` |
| 容器 ID | `ad4fc57c503e48f3040b617b8348a5fbf28b853d2d3a57752b067d6470af0343` |
| 镜像 | `openmmlab/lmdeploy:v0.14.0-cu12.8` |
| 镜像 SHA | `sha256:2826592346041666ffd071a15f3622837c34c463be83004639c28a28bdc29a08` |
| 状态 | Running |
| 启动时间 | `2026-08-11T05:52:08.843538564Z` |
| RestartCount | 0 |
| OOMKilled | false |
| 主命令 | `sleep infinity` |
| RestartPolicy | `no` |

容器“Running”只代表 `sleep infinity` 仍在运行，不代表 InternS2 API 服务可用。

### 5.2 容器运行配置

| 项目 | 已确认值 |
|---|---|
| WorkingDir | `/home/xl/interns2-finetune` |
| IPC | `host` |
| Shared memory | 68719476736 bytes，64 GiB |
| GPU | `NVIDIA_VISIBLE_DEVICES=all`，请求全部 GPU |
| NVIDIA capabilities | `compute,utility` |
| 网络 | 默认 `bridge` |
| 采样时容器 IP | `172.17.0.8`，不得作为固定配置 |
| PortBindings | 无 |
| Privileged | false |
| 容器内 Python | `3.12.3` |
| 镜像 CUDA | `12.8.1` |

当前没有发布 23333 端口。未来其他服务容器需要访问模型时，应将其加入同一个用户定义 Docker 网络，并确保 LMDeploy 监听 `0.0.0.0:23333`；不能依赖可能变化的 `172.17.0.8`。

### 5.3 挂载

| 宿主机路径 | 容器路径 | 权限 |
|---|---|---|
| `/home/xl` | `/home/xl` | 读写 |
| `/home/xl/interns2-finetune/models/Intern-S2-Preview-FP8` | `/models/Intern-S2-Preview-FP8` | 只读 |

当前容器明确挂载的是 FP8 模型。后续启动命令必须使用经过验证的实际模型路径，不应在文档和代码中默认猜测标准 BF16 模型。

### 5.4 当前服务检查结果

```text
Python 3.12.3
lmdeploy：未找到命令
curl: (7) Failed to connect to 127.0.0.1 port 23333
```

因此当前状态为：

- LMDeploy CLI 路径或 Python 包状态未知；
- InternS2 API Server 未监听 23333；
- `/v1/models` 未通过；
- 模型 ID 未记录；
- 容器日志没有提供 API Server 启动信息。

先在容器内执行以下只读诊断，不立即启动或重装任何包：

```bash
docker exec -it xl_interns2_lmdeploy /bin/bash
```

进入容器后执行：

```bash
cd /home/xl/interns2-finetune
command -v python3
command -v pip
command -v lmdeploy
python3 -m pip show lmdeploy
python3 -c "import lmdeploy; print(lmdeploy.__version__); print(lmdeploy.__file__)"
python3 -m lmdeploy --help
ps -ef
```

如果 `import lmdeploy` 成功但 CLI 不存在，说明可能只是入口脚本或 PATH 问题；如果导入也失败，则需确认当前镜像内为何没有预期包，再决定是否重建容器。不要在问题不清楚时直接修改这个已存在容器。

## 6. 端口与图形环境

### 6.1 待检查端口

计划使用：

| 端口 | 服务 |
|---:|---|
| 23333 | InternS2 LMDeploy |
| 8000 | agent-web |
| 8001 | robot-simulation |
| 8002 | planner-adapter |

`docker ps` 没有显示这些端口被发布，但仍可能有宿主机进程监听。需要执行：

```bash
ss -ltnp
```

重点检查 23333、8000、8001 和 8002。

### 6.2 图形与无头渲染

`nvidia-smi` 显示主机存在 Xorg 进程，但这不能证明未来仿真容器能使用 EGL、OpenGL 或 Xvfb。

宿主机补充检查：

```bash
command -v Xvfb
ldconfig -p
```

后续 Step 4 仍需要在独立仿真镜像内完成 SOFA 导入、OpenGL 上下文和 RGB 帧输出测试。宿主机有 Xorg 不等于容器离屏渲染已经可用。

## 7. Step 0 验收清单

| 验收项 | 状态 | 说明 |
|---|---|---|
| Git commit 已记录 | 通过 | `14c1256d...` |
| Git 既有改动已确认 | 未通过 | `agent/README.md` 和 `images/` 待确认 |
| Ubuntu/架构已记录 | 通过 | Ubuntu 24.04 系列、x86_64 |
| GPU/驱动/CUDA 已记录 | 通过 | 4 GPU、Driver 570.153.02、CUDA 12.8 |
| Docker Engine 可用 | 通过 | 28.2.2 |
| Docker Compose 可用 | 未通过 | `docker compose` 命令不存在 |
| InternS2 容器配置已记录 | 通过 | 镜像、挂载、GPU、IPC、网络已记录 |
| LMDeploy CLI 可用 | 未通过 | 容器内未找到命令 |
| InternS2 `/v1/models` 可访问 | 未通过 | 23333 无服务 |
| InternS2 模型 ID 已记录 | 未通过 | 等待 `/v1/models` |
| 计划端口可用 | 待确认 | 需要 `ss -ltnp` |
| EGL/OpenGL/Xvfb 已确认 | 待确认 | 留待补充检查和 Step 4 实测 |

Step 0 当前判定：**进行中，尚未通过**。

## 8. 下一轮只读检查命令

为了避免输出再次被完整 `docker inspect` 淹没，下一轮只需执行以下命令并返回结果。

### 8.1 服务器宿主机

```bash
cd ~/interns2-finetune
git diff -- agent/README.md
find images -maxdepth 2 -type f -print
nvidia-smi --query-gpu=index,name,memory.total,driver_version --format=csv
docker-compose version
apt-cache policy docker-compose-v2
apt-cache policy docker-compose-plugin
ss -ltnp
command -v Xvfb
```

如果某条命令没有输出或提示未找到，原样保留结果即可，不需要先行安装。

### 8.2 InternS2 容器内

```bash
docker exec -it xl_interns2_lmdeploy /bin/bash
```

进入容器后：

```bash
cd /home/xl/interns2-finetune
command -v python3
command -v pip
command -v lmdeploy
python3 -m pip show lmdeploy
python3 -c "import lmdeploy; print(lmdeploy.__version__); print(lmdeploy.__file__)"
python3 -m lmdeploy --help
ps -ef
```

完成后执行：

```bash
exit
```

以上均为只读诊断。获得结果后再决定 Compose 安装方式和 InternS2 服务恢复方式。

